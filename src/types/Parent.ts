export interface ParentInterface {
  name: string;
  tahun_lahir?: number | null; // Optional with possible null value
  jenjang_pendidikan: string;
  pekerjaan: string;
  penghasilan: string;
  NIK: string;
}
